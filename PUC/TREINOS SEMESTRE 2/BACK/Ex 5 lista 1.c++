#include <iostream>

using namespace std;

//Ex 5

int main()
{
    int altura, base, area;
    cout<<"Digite a altura em numero real: ";
    cin>>altura;
    cout<<"Digite a base em numero real: ";
    cin>>base;
    area = base * altura;
    cout<<"Altura: " << altura << " Base: " << base << " Area: " << area << endl;

    return 0;
}