import React from 'react';
import ReactDOM from 'react-dom/client';
import './index.css';
import App from './App';
import reportWebVitals from './reportWebVitals';

// Definimos uma Raiz de renderrizacao e estamos renderizando componente por componente. A raiz sera a div com id Root
const root = ReactDOM.createRoot(document.getElementById('root')); //DOM element is the div with ID = "root"
root.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);

// If you want to start measuring performance in your app, pass a function
// to log results (for example: reportWebVitals(console.log))
// or send to an analytics endpoint. Learn more: https://bit.ly/CRA-vitals
reportWebVitals();
